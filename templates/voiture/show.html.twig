{% extends 'base.html.twig' %} {% block title %}Voiture{% endblock %} {% block
content %} <br /><br />
<div class="row">
  <div class="col-sm-3">
    <a href="{{ path('geo') }}" class="btn btn-primary">Map</a>
  </div>
  <div class="col-md-6"></div>
  <div class="col-sm-1"></div>
</div>
<h3><center>Afficher une Voiture</center></h3>

<table class="table table-striped table-bordered table-hover">
  <tbody>
    <tr>
      <th>Numéro Voiture</th>
      <td>{{ voiture.numVoiture }}</td>
    </tr>
    <tr>
      <th>Type de Voiture</th>
      <td>{{ voiture.typeVoiture }}</td>
    </tr>
    <tr>
      <th>Ligne</th>
      <td>{{ voiture.ligne }}</td>
    </tr>
    <tr>
      <th>Destination</th>
      <td>{{ voiture.destination }}</td>
    </tr>
  </tbody>
</table>

<div class="row">
  <div class="col-sm-3">
    <a href="{{ path('voiture') }}" class="btn btn-warning">Back to list</a>
  </div>
  <div class="col-md-6"></div>
  <div class="col-sm-1">
    <a
      href="{{ path('editVoiture', { id: voiture.id }) }}"
      class="btn btn-secondary"
      >edit</a
    >
  </div>
  <div class="col-sm-2">{{ include("voiture/_delete_form.html.twig") }}</div>
</div>
<br /><br />
{% endblock %}
