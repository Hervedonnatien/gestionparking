{% extends 'base.html.twig' %} {% block title %}Voiture{% endblock %} {% block
content %}
<h3><center>Afficher une Voiture</center></h3>

<table class="table table-striped table-bordered table-hover">
  <tbody>
    <tr>
      <th>Numéro Parking</th>
      <td>{{ parking.numParking }}</td>
    </tr>

    <tr>
      <th>Numéro Voiture</th>
      <td>{{ parking.numVoiture }}</td>
    </tr>

    <tr>
      <th>Reservation</th>
      <td>{{ parking.reservation }}</td>
    </tr>
  </tbody>
</table>

<div class="row">
  <div class="col-sm-3">
    <a href="{{ path('parking') }}" class="btn btn-warning">Back to list</a>
  </div>
  <div class="col-md-6"></div>
  <div class="col-sm-1">
    <a
      href="{{ path('editParking', { id: parking.id }) }}"
      class="btn btn-secondary"
      >edit</a
    >
  </div>
  <div class="col-sm-2">{{ include("parking/_delete_form.html.twig") }}</div>
</div>
<br /><br />
{% endblock %}
