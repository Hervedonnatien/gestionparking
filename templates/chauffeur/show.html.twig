{% extends 'base.html.twig' %} {% block title %}Voiture{% endblock %} {% block
content %}
<h3><center>Afficher une Voiture</center></h3>

<table class="table table-striped table-bordered table-hover">
  <tbody>
    <tr>
      <th>Nom Chauffeur</th>
      <td>{{ chauffeur.nomChauffeur }}</td>
    </tr>

    <tr>
      <th>Numéro Chauffeur</th>
      <td>{{ chauffeur.numChauffeur }}</td>
    </tr>

    <tr>
      <th>Numéro Voiture</th>
      <td>{{ chauffeur.numVoiture }}</td>
    </tr>
  </tbody>
</table>

<div class="row">
  <div class="col-sm-3">
    <a href="{{ path('chauffeur') }}" class="btn btn-warning">Back to list</a>
  </div>
  <div class="col-md-6"></div>
  <div class="col-sm-1">
    <a
      href="{{ path('editChauffeur', { id: chauffeur.id }) }}"
      class="btn btn-secondary"
      >edit</a
    >
  </div>
  <div class="col-sm-2">{{ include("chauffeur/_delete_form.html.twig") }}</div>
</div>
<br /><br />
{% endblock %}
